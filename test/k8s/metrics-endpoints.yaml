- endpoint: http://localhost:9090
  alerts:
  - alerts.yml
- endpoint: http://localhost:9090
  alias: local-indexing
  indexer:
    type: local
    metricsDirectory: {{ .METRICS_FOLDER }}
  metrics:
  - metrics-profile.yaml
