kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ .JobName }}-{{ .Iteration }}-{{ .Replica }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .size }}