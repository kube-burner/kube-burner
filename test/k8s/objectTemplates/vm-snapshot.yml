apiVersion: snapshot.kubevirt.io/v1beta1
kind: VirtualMachineSnapshot
metadata:
  name: vm-snapshot-{{ .Replica }}
spec:
  source:
    apiGroup: kubevirt.io
    kind: VirtualMachine
    name: base-vm
